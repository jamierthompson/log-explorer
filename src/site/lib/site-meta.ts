/*
 * Title metadata shared by the static document head and the client-side
 * view switcher, so the per-view titles can't drift from the base title.
 */
export const SITE_NAME = "Log Explorer";
export const SITE_TITLE = "Log Explorer — an incident, investigated in place";
export const SITE_DESCRIPTION =
  "13:31 UTC: checkout starts returning 503s. Investigate a live incident in a log explorer that opens context in place — no new tab for every look, no scattered timeline — then read how it was built.";
