/** The root-cause call's path. Shared so the link that opens it and the
 * modal slot that dismisses itself when the path moves on can't drift
 * apart. */
export const ROOT_CAUSE_PATH = "/demo/root-cause";
